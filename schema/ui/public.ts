/**
 * Public web app entrypoint.
 *
 * Keep this intentionally narrow so customer-facing bundles only include the
 * public shell and its transitive components.
 */
export { ObApp } from "./components/ob-app";
