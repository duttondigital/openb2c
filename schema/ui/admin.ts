/**
 * Admin dashboard entrypoint.
 *
 * Keep this separate from the public entrypoint so admin data-management
 * components are only bundled into the admin app.
 */
export { ObAdminApp } from "./components/ob-admin-app";
