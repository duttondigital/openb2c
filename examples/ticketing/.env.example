# Ticketing generated app environment
# Fill values in deployment secrets; do not commit real secret values.

NODE_ENV=development
DB_PATH=./data/openb2c-ticketing.db
PORT=3095
MCP_PORT=3096
AUTH_ENABLED=true
CORS_ORIGINS=https://ticketing.example

# Provide one registry mode in production.
REGISTRY_PRIVATE_KEY=
REGISTRY_PUBLIC_KEY=
ALLOW_EPHEMERAL_REGISTRY_KEYS=false

# Declared email effects.
EMAIL_WEBHOOK_URL=

MAX_REQUEST_BODY_BYTES=1048576
MAX_PAGE_LIMIT=1000
ROUTE_TIMEOUT_MS=30000
EFFECT_MAX_ATTEMPTS=3
EFFECT_RETRY_DELAY_MS=1000
